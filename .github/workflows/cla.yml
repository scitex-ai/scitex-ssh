name: CLA Assistant

# PILOT: org-level reusable-workflow caller (scitex-ai/.github, 2026-07-10).
# Triggers/permissions preserved verbatim from the prior local file.
# Reusable workflow defaults (owner_login=ywatanabe1989,
# owner_allowlist="bot*,ywatanabe1989", default_branch=main) already match
# this repo's prior hardcoded values, so no `with:` overrides are needed.
# This repo's `GH_PERSONAL_ACCESS_TOKEN` secret already matches the name
# the reusable workflow expects.

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  call:
    uses: scitex-ai/.github/.github/workflows/cla.yml@main
    # Same reason as ci.yml: the shared workflow still defaults runs_on to
    # `spartan-cpu`, and those runners are offline. Naming the destination
    # is also what PS-224 (runner-destination-unregistered) asks for — it
    # was the ONLY error left in this repo's audit, and it flagged a call
    # that had been silently relying on that default.
    with:
      runs_on: ${{ vars.CI_RUNS_ON }}
    secrets: inherit
